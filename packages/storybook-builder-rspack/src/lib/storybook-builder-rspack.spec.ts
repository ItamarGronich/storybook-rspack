import { storybookBuilderRspack } from './storybook-builder-rspack';

describe('storybookBuilderRspack', () => {
  it('should work', () => {
    expect(storybookBuilderRspack()).toEqual('storybook-builder-rspack');
  });
});
