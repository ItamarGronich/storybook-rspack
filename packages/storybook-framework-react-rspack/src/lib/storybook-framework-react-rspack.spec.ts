import { storybookFrameworkReactRspack } from './storybook-framework-react-rspack';

describe('storybookFrameworkReactRspack', () => {
  it('should work', () => {
    expect(storybookFrameworkReactRspack()).toEqual(
      'storybook-framework-react-rspack'
    );
  });
});
